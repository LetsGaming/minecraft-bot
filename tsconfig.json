{
  // Solution-style root: no files of its own, just the project graph.
  // `tsc -b` from the root builds everything; editors resolve each file
  // through its workspace's own tsconfig.
  "files": [],
  "references": [
    { "path": "src/schema" },
    { "path": "src/core" },
    { "path": "src/bot" },
    { "path": "src/web" }
  ]
}
